import './bootstrap';
// LIBRERIA DE FUNCIONALIDADES Y TRANSACCIONES VIUALES GSAP
import { gsap } from "gsap";
import { ScrollTrigger } from "gsap/ScrollTrigger";

const EIgsap = gsap
const EIScrollTrigger = ScrollTrigger

export {
    EIgsap,
    EIScrollTrigger
}
